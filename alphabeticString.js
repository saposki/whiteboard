function alphabetic(str){
   var x = /[A-Z]/;


      // if(charAt(i) is alpahbet)
      // if(str.search(x) == -1){
      // check charAt(i-1) = '+'
      // check charAt(i+1) = '+'

      // tool kit
      //    regular expressions ?

};

alphabetic('a+a+a+');
